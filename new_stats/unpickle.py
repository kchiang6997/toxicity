import pickle

print("lstm word2vec 150")
with open('lstm_word2vec_epoch_5_150d_f1s.p', 'rb') as f21:
    # The protocol version used is detected automatically, so we do not
    # have to specify it.
    data = pickle.load(f21)
    print("f1")
    print(data)

with open('lstm_word2vec_epoch_5_150d_nbatch.p', 'rb') as f22:
	# The protocol version used is detected automatically, so we do not
	# have to specify it.
	data = pickle.load(f22)
	print("nbatch")
	print(data)

with open('lstm_word2vec_epoch_5_150d_precisions.p', 'rb') as f23:
	# The protocol version used is detected automatically, so we do not
	# have to specify it.
	data = pickle.load(f23)
	print("precision")
	print(data)

with open('lstm_word2vec_epoch_5_150d_recalls.p', 'rb') as f24:
	# The protocol version used is detected automatically, so we do not
	# have to specify it.
	data = pickle.load(f24)
	print("recalls")
	print(data)

with open('lstm_word2vec_epoch_5_150d_losses.p', 'rb') as f25:
	# The protocol version used is detected automatically, so we do not
	# have to specify it.
	data = pickle.load(f25)
	print("losses")
	print(data)

with open('lstm_word2vec_epoch_5_150d_accs.p', 'rb') as f26:
	# The protocol version used is detected automatically, so we do not
	# have to specify it.
	data = pickle.load(f26)
	print("accs")
	print(data)

print("gru word2vec 150")
with open('gru_word2vec_epoch_5_150d_f1s.p', 'rb') as f21:
    # The protocol version used is detected automatically, so we do not
    # have to specify it.
    data = pickle.load(f21)
    print("f1")
    print(data)

with open('gru_word2vec_epoch_5_150d_nbatch.p', 'rb') as f22:
	# The protocol version used is detected automatically, so we do not
	# have to specify it.
	data = pickle.load(f22)
	print("nbatch")
	print(data)

with open('gru_word2vec_epoch_5_150d_precisions.p', 'rb') as f23:
	# The protocol version used is detected automatically, so we do not
	# have to specify it.
	data = pickle.load(f23)
	print("precision")
	print(data)

with open('gru_word2vec_epoch_5_150d_recalls.p', 'rb') as f24:
	# The protocol version used is detected automatically, so we do not
	# have to specify it.
	data = pickle.load(f24)
	print("recalls")
	print(data)

with open('gru_word2vec_epoch_5_150d_losses.p', 'rb') as f25:
	# The protocol version used is detected automatically, so we do not
	# have to specify it.
	data = pickle.load(f25)
	print("losses")
	print(data)

with open('gru_word2vec_epoch_5_150d_accs.p', 'rb') as f26:
	# The protocol version used is detected automatically, so we do not
	# have to specify it.
	data = pickle.load(f26)
	print("accs")
	print(data)

print("lstm glove 150")
with open('lstm_glove_epoch_5_150d_f1s.p', 'rb') as f21:
    # The protocol version used is detected automatically, so we do not
    # have to specify it.
    data = pickle.load(f21)
    print("f1")
    print(data)

with open('lstm_glove_epoch_5_150d_nbatch.p', 'rb') as f22:
	# The protocol version used is detected automatically, so we do not
	# have to specify it.
	data = pickle.load(f22)
	print("nbatch")
	print(data)

with open('lstm_glove_epoch_5_150d_precisions.p', 'rb') as f23:
	# The protocol version used is detected automatically, so we do not
	# have to specify it.
	data = pickle.load(f23)
	print("precision")
	print(data)

with open('lstm_glove_epoch_5_150d_recalls.p', 'rb') as f24:
	# The protocol version used is detected automatically, so we do not
	# have to specify it.
	data = pickle.load(f24)
	print("recalls")
	print(data)

with open('lstm_glove_epoch_5_150d_losses.p', 'rb') as f25:
	# The protocol version used is detected automatically, so we do not
	# have to specify it.
	data = pickle.load(f25)
	print("losses")
	print(data)

with open('lstm_glove_epoch_5_150d_accs.p', 'rb') as f26:
	# The protocol version used is detected automatically, so we do not
	# have to specify it.
	data = pickle.load(f26)
	print("accs")
	print(data)

print("gru glove 150")
with open('gru_glove_epoch_5_150d_f1s.p', 'rb') as f21:
    # The protocol version used is detected automatically, so we do not
    # have to specify it.
    data = pickle.load(f21)
    print("f1")
    print(data)

with open('gru_glove_epoch_5_150d_nbatch.p', 'rb') as f22:
	# The protocol version used is detected automatically, so we do not
	# have to specify it.
	data = pickle.load(f22)
	print("nbatch")
	print(data)

with open('gru_glove_epoch_5_150d_precisions.p', 'rb') as f23:
	# The protocol version used is detected automatically, so we do not
	# have to specify it.
	data = pickle.load(f23)
	print("precision")
	print(data)

with open('gru_glove_epoch_5_150d_recalls.p', 'rb') as f24:
	# The protocol version used is detected automatically, so we do not
	# have to specify it.
	data = pickle.load(f24)
	print("recalls")
	print(data)

with open('gru_glove_epoch_5_150d_losses.p', 'rb') as f25:
	# The protocol version used is detected automatically, so we do not
	# have to specify it.
	data = pickle.load(f25)
	print("losses")
	print(data)

with open('gru_glove_epoch_5_150d_accs.p', 'rb') as f26:
	# The protocol version used is detected automatically, so we do not
	# have to specify it.
	data = pickle.load(f26)
	print("accs")
	print(data)